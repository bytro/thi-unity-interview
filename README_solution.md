# thi-unity-interview - My Solution

The version works exactly like the original.

- To make setting the threshold for the depth edge detection a little easier, I defaulted to a linear space inside the shader. So the value should be more towards the upper end of the range.
